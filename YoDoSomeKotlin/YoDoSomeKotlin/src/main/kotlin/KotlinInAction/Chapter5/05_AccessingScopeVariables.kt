package com.vav.KotlinInAction.Chapter5

/*
    We can access and modify the local variables through the lambda context

    In java, we were only restricted to use final variables in the lambda so they cannot be
    changed.
    In kotlin we can also use non-final variables. This is called capturing a variable in the lambda.




 */